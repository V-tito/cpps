function main(A:array[integer]  returns integer)
   let
      s := 0
   in
      for i in 1, 100
      repeat
         s := old s + A[i]
      returns value of s
      end for
   end let
end function
